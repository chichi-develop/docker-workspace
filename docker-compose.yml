version: '3.3'

services:
  workspace:
    build:
      context: .
#      args:
#        - NODE_ENV=development

    container_name: workspace
    #ports:
      #- "8338:3001"
    tty: true

    volumes:
      - ./share:/share:delegated

networks:
  default:
    external:
      name: chichi-network
